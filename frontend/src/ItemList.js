import { useEffect } from "react";

const ItemList = (props) => {

    // useEffect(() => {
    //     fetch("")
    //         .then((response) => )
    // }, []);

    return <h1>{props.title}</h1>
}

export default ItemList;